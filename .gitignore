# Built executables and local build outputs
bin/
dist/
build/
out/

# OS/arch-specific binaries (already have some — keep these for safety)
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test

# Coverage/test artifacts (you already have some of these)
*.out
coverage.*
*.coverprofile
profile.cov